<Window x:Class="DynamicWallpaper.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DynamicWallpaper.UI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Dynamic Wallpaper" Height="450" Width="800"
    >
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#4B0082" Offset="0"/>
            <!-- Roxo escuro -->
            <GradientStop Color="#000000" Offset="1"/>
            <!-- Preto -->
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Border CornerRadius="10" Padding="15" Background="{DynamicResource MaterialDesignCardBackground}" >
            <Grid>
                <!-- Botões no canto superior direito -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                    <Button ToolTip="Atualizar Wallpapers"
                            Click="RefreshWallpapers"
                            Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="Refresh" Height="24" Width="24"/>
                    </Button>
                    <Button ToolTip="Configurações"
                            Click="OpenSettings"
                            Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="Settings" Height="24" Width="24"/>
                    </Button>
                    <Button ToolTip="Selecionar Wallpaper"
                            Click="SelectWallpaper"
                            Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="FolderImage" Height="24" Width="24"/>
                    </Button>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto" DockPanel.Dock="Bottom">
                    <WrapPanel x:Name="WallpaperPanel" Margin="10" Opacity="0" Loaded="OnWallpapersLoaded"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
